import React from 'react'

import ISI from '../../components/shared/ISI/index'

export default {
  title: 'Base/ISI',
  component: ISI,
}

const Template = (args) => <ISI {...args} />

export const Default = Template.bind({})
